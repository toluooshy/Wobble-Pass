"use es6";

import React, { useEffect, useState } from "react";
import { DESKTOP_MIN } from "../utils/Constants";

const ExplorePage = ({ dimensions }) => {
  return (
    <div
      style={{
        padding: "80px 0px 40px 0px",
      }}
    >
      MORE COMING SOON
    </div>
  );
};

export default ExplorePage;
